// Constantes inyectadas por Vite en tiempo de build (ver vite.config.ts,
// `define`) — no existen en tiempo de ejecución como variables reales, el
// bundler las sustituye por su valor literal al compilar.
declare const __APP_VERSION__: string
declare const __BUILD_DATE__: string
